[project]
name = "policylens"
version = "0.1.0"
description = "PolicyLens FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "api:app"